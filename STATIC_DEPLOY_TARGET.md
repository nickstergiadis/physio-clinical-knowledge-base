# Static deploy target

Target deployment: GitHub Pages

Constraints:
- App must deploy as a fully static site.
- No runtime database.
- No server-only features at runtime.
- Source of truth remains /knowledge_base_source markdown files.
- Search must work client-side.
- Favorites/saved pages can use localStorage.
- Site must work under a GitHub Pages project path, not root domain.
